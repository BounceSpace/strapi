HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified

# Netlify Deployments Plugin Configuration
# Get these values from Netlify:
# 1. Access Token: https://app.netlify.com/user/applications (create a new personal access token)
# 2. Site ID: Found in Site settings > General > Site details (Site information section)
# 3. Build Hook: Site settings > Build & deploy > Build hooks (create a new build hook)
# 4. Branch: The branch to deploy (usually "master" or "main")
NETLIFY_DEPLOYMENTS_PLUGIN_ACCESS_TOKEN=
NETLIFY_SITE_NAME=bouncespace.co
NETLIFY_SITE_ID=
NETLIFY_BUILD_HOOK=
NETLIFY_BRANCH=master
